version: '3.8'
services:
  api:
    container_name: quake-log-parser
    build:
      context: .
      dockerfile: Dockerfile
      target: run
    hostname: balder
    ports:
      - "3007:3007"
    restart: on-failure